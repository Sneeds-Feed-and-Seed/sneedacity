#!/bin/bash
# Run this script to take an xpm generated by the gimp and format it for
# use Sneedacity
for arg in $@
do
  cat $arg| sed -e 's/\_xpm\[\]/\[\]/' -e 's/^static char \*/static const char  \*/' > tmp
  mv  tmp $arg

done
